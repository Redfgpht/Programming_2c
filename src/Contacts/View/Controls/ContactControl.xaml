<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">

    <UserControl.Resources>
        <ControlTemplate x:Key="EmptyValidationTemplate">
            <AdornedElementPlaceholder/>
        </ControlTemplate>

        <!-- Стиль для текстовых полей с валидацией -->
        <Style x:Key="ErrorTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#ABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource EmptyValidationTemplate}"/>

            <Style.Triggers>
                <!-- Триггер при наличии ошибки валидации -->
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="Background" Value="#FFE0E0"/>
                    <Setter Property="ToolTip" 
                            Value="{Binding RelativeSource={RelativeSource Self}, 
                            Path=(Validation.Errors)/ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel Margin="0,0,0,50">
            <Border Width="100" 
                    Height="100" 
                    BorderBrush="Gray" 
                    BorderThickness="1" 
                    HorizontalAlignment="Left"
                    Margin="0,0,0,10"
                    Background="#F5F5F5">
                <TextBlock Text="📷" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           FontSize="24"/>
            </Border>

            <!-- Поле Name -->
            <TextBlock Text="Имя:" Margin="0,0,0,3"/>
            <TextBox Name="NameTextBox"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}"
                     IsReadOnly="{Binding DataContext.IsViewMode, RelativeSource={RelativeSource AncestorType=Window}}"
                     Style="{StaticResource ErrorTextBoxStyle}"/>

            <!-- Поле Phone -->
            <TextBlock Text="Телефон:" Margin="0,0,0,3"/>
            <TextBox Name="PhoneTextBox"
                     Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}"
                     IsReadOnly="{Binding DataContext.IsViewMode, RelativeSource={RelativeSource AncestorType=Window}}"
                     Style="{StaticResource ErrorTextBoxStyle}"
                     PreviewTextInput="PhoneTextBox_PreviewTextInput"
                     DataObject.Pasting="PhoneTextBox_Pasting"/>

            <!-- Поле Email -->
            <TextBlock Text="Почта:" Margin="0,0,0,3"/>
            <TextBox Name="EmailTextBox"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}"
                     IsReadOnly="{Binding DataContext.IsViewMode, RelativeSource={RelativeSource AncestorType=Window}}"
                     Style="{StaticResource ErrorTextBoxStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>